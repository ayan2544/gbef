/* define $ as jQuery just in case */
				(function($) {
					/* doc ready */
					$(function() {
						/* init the plugin */
						$('.inner-events-video-sec').thumb_slider();
					});
				})(jQuery);